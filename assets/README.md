# Bundled data assets

- `book.bin` — Polyglot opening book. Not committed; build locally with `scripts/build-book.sh` (Lichess masters DB → Polyglot).
- `eco.json` — ECO code → opening name mapping. Will land in Phase 1.
- `nnue/` — Stockfish NNUE weights, if the Stockfish path wins the engine decision.

All chosen sources are CC0 / permissive / public domain (see `PLAN.md` → "Data Sources").
