#!/usr/bin/env bash
# Build Stockfish as a static fat (arm64 + x86_64) lib for macOS.
# Populated in Phase 2 once the engine decision lands.
set -euo pipefail
echo "stockfish/build-macos.sh: not implemented yet (Phase 2)." >&2
exit 1
