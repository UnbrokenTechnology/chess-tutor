#!/usr/bin/env bash
# Build Stockfish as a static lib for iOS (device + simulator).
# Populated in Phase 2 once the engine decision lands.
set -euo pipefail
echo "stockfish/build-ios.sh: not implemented yet (Phase 2)." >&2
exit 1
